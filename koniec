def run_program(name/ params):
        print(params)
        if params[0] == 'push':
                pass

if __name__=='__main__':
        args = sys.argv
        run_program('PyCharm', sys.argv[1:])

